import React from 'react';

const Resume: React.FC = () => {
    return (
        <section className="component-container">
            <h1>Section: Resume</h1>
        </section>
    );
};

export default Resume;
