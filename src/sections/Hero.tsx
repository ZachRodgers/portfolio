import React from 'react';
import './Hero.css';

const Hero: React.FC = () => {
    return (
        <section className="component-container">
            <h1 className="hero-temp-spacer">Section: Hero</h1>
        </section>
    );
};

export default Hero;
