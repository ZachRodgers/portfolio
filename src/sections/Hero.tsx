import React from 'react';

const Hero: React.FC = () => {
    return (
        <section className="component-container">
            <h1>Section: Hero</h1>
        </section>
    );
};

export default Hero;
