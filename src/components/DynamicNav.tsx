import React from 'react';

const DynamicNav: React.FC = () => {
    return (
        <nav className="component-container">
            <h2>Component: DynamicNav</h2>
        </nav>
    );
};

export default DynamicNav;
